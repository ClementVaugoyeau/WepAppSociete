@model IEnumerable<BackHorodatage.Models.HorodatageUser>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateArrival)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateDeparture)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdUser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateArrival)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateDeparture)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdHorodatage">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdHorodatage">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdHorodatage">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
